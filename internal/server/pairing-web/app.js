const button=document.querySelector('#pair');const statusText=document.querySelector('#status');const result=document.querySelector('#result');const details=document.querySelector('#details');const fingerprint=document.querySelector('#fingerprint');
const setStatus=(text,state='')=>{statusText.textContent=text;result.className=`result ${state}`};
const base64url=bytes=>{let binary='';for(const byte of new Uint8Array(bytes))binary+=String.fromCharCode(byte);return btoa(binary).replaceAll('+','-').replaceAll('/','_').replaceAll('=','')};
const randomID=prefix=>`${prefix}_${base64url(crypto.getRandomValues(new Uint8Array(18)))}`;
const parseSecret=()=>{const raw=location.hash.slice(1);const split=raw.indexOf('.');if(split<1)return null;return{pairingId:raw.slice(0,split),secret:raw.slice(split+1)}};
const openDB=()=>new Promise((resolve,reject)=>{const request=indexedDB.open('yuanshu-control-client',1);request.onupgradeneeded=()=>request.result.createObjectStore('keys');request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(new Error('storage unavailable'))});
const storeKey=async(id,value)=>{const db=await openDB();await new Promise((resolve,reject)=>{const tx=db.transaction('keys','readwrite');tx.objectStore('keys').put(value,id);tx.oncomplete=resolve;tx.onerror=()=>reject(new Error('storage unavailable'))});db.close()};
const deleteKey=async id=>{const db=await openDB();await new Promise((resolve,reject)=>{const tx=db.transaction('keys','readwrite');tx.objectStore('keys').delete(id);tx.oncomplete=resolve;tx.onerror=()=>reject(new Error('storage unavailable'))});db.close()};
const poll=async(pairing,client)=>{for(let attempt=0;attempt<150;attempt++){await new Promise(resolve=>setTimeout(resolve,2000));const response=await fetch(`/v1/control-client-pairings/${encodeURIComponent(pairing.pairingId)}/status`,{headers:{Authorization:`Bearer ${pairing.secret}`},cache:'no-store'});if(!response.ok)throw new Error('status unavailable');const value=await response.json();if(value.status==='approved'){await storeKey('active',{...client,ownerId:value.ownerId,nodeId:value.nodeId,nodePublicKey:value.nodePublicKey,proof:value.proof});await deleteKey(`pending:${pairing.pairingId}`);location.hash='';setStatus('已连接，可以关闭此页面','done');return}if(value.status==='declined'||value.status==='expired'){await deleteKey(`pending:${pairing.pairingId}`);throw new Error(value.status)}}throw new Error('expired')};
button.addEventListener('click',async()=>{const pairing=parseSecret();if(!pairing){setStatus('请从办公室电脑重新生成配对链接','error');return}const name=document.querySelector('#name').value.trim();if(!name){setStatus('请输入设备名称','error');return}button.disabled=true;let claimed=false;try{if(!crypto.subtle||!indexedDB)throw new Error('unsupported');const keys=await crypto.subtle.generateKey({name:'Ed25519'},false,['sign','verify']);const publicKey=base64url(await crypto.subtle.exportKey('raw',keys.publicKey));const client={clientId:randomID('cli'),keyId:randomID('key'),name,publicKey,privateKey:keys.privateKey};await storeKey(`pending:${pairing.pairingId}`,client);const response=await fetch(`/v1/control-client-pairings/${encodeURIComponent(pairing.pairingId)}/claim`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${pairing.secret}`},body:JSON.stringify({clientId:client.clientId,keyId:client.keyId,name,publicKey})});if(!response.ok)throw new Error('claim failed');claimed=true;const value=await response.json();fingerprint.textContent=value.fingerprint;details.hidden=false;setStatus('等待办公室电脑确认此指纹','waiting');await poll(pairing,client)}catch(error){if(!claimed)await deleteKey(`pending:${pairing.pairingId}`).catch(()=>{});const text=error instanceof Error&&error.message==='declined'?'办公室电脑拒绝了请求':'配对未完成，请重新生成链接';setStatus(text,'error');button.disabled=false}});
if(parseSecret())setStatus('配对链接已就绪');
