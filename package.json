{
  "name": "yuanshu-workspace",
  "private": true,
  "scripts": {
    "brand:sync": "node scripts/brand-assets.mjs sync",
    "brand:check": "node scripts/brand-assets.mjs check",
    "build": "pnpm brand:sync && pnpm i18n:generate && pnpm --dir web build && pnpm --dir node-web build",
    "i18n:check": "node scripts/i18n.mjs check",
    "i18n:generate": "node scripts/i18n.mjs generate",
    "protocol:check": "node scripts/protocol.mjs check && node scripts/protocol-v11.mjs check",
    "protocol:generate": "node scripts/protocol.mjs generate && node scripts/protocol-v11.mjs generate",
    "protocol:test": "node scripts/protocol-test.mjs && node scripts/protocol-v11-test.mjs",
    "readme:check": "node scripts/readme-check.mjs",
    "status:check": "node scripts/status-catalog.mjs check",
    "status:generate": "node scripts/status-catalog.mjs generate",
    "test": "pnpm --dir web test && pnpm --dir node-web test"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "quicktype": "26.0.0"
  }
}
