{
  "name": "yuanshu-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm --dir web build && pnpm --dir node-web build",
    "protocol:check": "node scripts/protocol.mjs check",
    "protocol:generate": "node scripts/protocol.mjs generate",
    "protocol:test": "node scripts/protocol-test.mjs",
    "status:check": "node scripts/status-catalog.mjs check",
    "status:generate": "node scripts/status-catalog.mjs generate",
    "test": "pnpm --dir web test && pnpm --dir node-web test"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "quicktype": "26.0.0"
  }
}
