{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yuanshu.ai/schemas/config/v2/node-config.schema.json",
  "title": "Yuanshu Node Configuration v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["config_version", "host", "transport", "relay", "identity", "agent_instances", "events", "workspaces"],
  "properties": {
    "config_version": { "const": 2 },
    "host": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 128 },
        "locale": { "enum": ["zh-CN", "en-US"] }
      }
    },
    "transport": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode"],
      "properties": { "mode": { "enum": ["relay", "standalone"] } }
    },
    "relay": { "$ref": "#/$defs/relay" },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "oneOf": [
        { "required": ["key_file"], "not": { "required": ["private_key_ref"] } },
        { "required": ["private_key_ref"], "not": { "required": ["key_file"] } }
      ],
      "properties": {
        "key_file": { "const": "identity.key" },
        "private_key_ref": { "$ref": "#/$defs/secretRef" }
      }
    },
    "agent_instances": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/agentInstance" }
    },
    "events": { "$ref": "#/$defs/events" },
    "workspaces": { "type": "array", "items": { "$ref": "#/$defs/workspace" } }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "transport": {
            "properties": { "mode": { "const": "relay" } },
            "required": ["mode"]
          }
        }
      },
      "then": {
        "properties": {
          "relay": { "properties": { "url": { "minLength": 1 } } }
        }
      }
    }
  ],
  "$defs": {
    "secretRef": {
      "type": "string",
      "maxLength": 256,
      "pattern": "^[^\\x00-\\x1F\\x7F]*$"
    },
    "relay": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url", "proxy_url", "connect_timeout_seconds", "credential_ref", "proxy_credential_ref"],
      "properties": {
        "url": {
          "anyOf": [
            { "const": "" },
            { "type": "string", "format": "uri", "pattern": "^wss://" },
            { "type": "string", "format": "uri", "pattern": "^ws://(?:127\\.0\\.0\\.1|\\[::1\\])(?::[0-9]+)?(?:/|$)" }
          ]
        },
        "proxy_url": {
          "anyOf": [
            { "const": "" },
            { "type": "string", "format": "uri", "pattern": "^(https?|socks5)://" }
          ]
        },
        "ca_bundle_file": { "type": "string", "maxLength": 4096 },
        "connect_timeout_seconds": { "type": "integer", "minimum": 1, "maximum": 120 },
        "credential_ref": { "$ref": "#/$defs/secretRef" },
        "proxy_credential_ref": { "$ref": "#/$defs/secretRef" }
      }
    },
    "codex": {
      "type": "object",
      "additionalProperties": false,
      "required": ["enabled", "binary", "runtime_mode", "home"],
      "properties": {
        "enabled": { "type": "boolean" },
        "binary": { "type": "string", "maxLength": 4096 },
        "runtime_mode": { "const": "stdio" },
        "home": { "type": "string", "maxLength": 4096 }
      },
      "allOf": [
        {
          "if": { "properties": { "enabled": { "const": true } } },
          "then": { "properties": { "binary": { "minLength": 1 } } }
        }
      ]
    },
    "agentInstance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "adapter_type", "display_name", "enabled", "is_default", "runtime_mode"],
      "properties": {
        "id": { "type": "string", "minLength": 1, "maxLength": 128 },
        "adapter_type": { "enum": ["codex", "claude-code", "opencode"] },
        "display_name": { "type": "string", "minLength": 1, "maxLength": 128 },
        "enabled": { "type": "boolean" },
        "is_default": { "type": "boolean" },
        "runtime_mode": { "enum": ["managed", "detected-only"] },
        "codex": { "$ref": "#/$defs/codex" }
      }
    },
    "events": {
      "type": "object",
      "additionalProperties": false,
      "required": ["max_age_hours", "max_size_mib"],
      "properties": {
        "max_age_hours": { "type": "integer", "minimum": 1, "maximum": 720 },
        "max_size_mib": { "type": "integer", "minimum": 16, "maximum": 4096 }
      }
    },
    "workspace": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "display_name", "path", "allowed_agent_instances", "default_agent_instance", "permission_profile", "allow_network"],
      "properties": {
        "id": { "type": "string", "minLength": 1, "maxLength": 128 },
        "display_name": { "type": "string", "minLength": 1, "maxLength": 128 },
        "path": { "type": "string", "minLength": 1, "maxLength": 4096 },
        "allowed_agent_instances": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1, "maxLength": 128 }
        },
        "default_agent_instance": { "type": "string", "minLength": 1, "maxLength": 128 },
        "permission_profile": { "enum": ["read-only", "workspace-write"] },
        "allow_network": { "type": "boolean" }
      }
    }
  }
}
